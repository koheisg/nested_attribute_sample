<%= form_with(model: job_schedule, local: true) do |form| %>
  <% if job_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_schedule.errors.count, "error") %> prohibited this job_schedule from being saved:</h2>

      <ul>
      <% job_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :job_id %>
    <%= form.text_field :job_id, id: :job_schedule_job_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :job_schedule_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
